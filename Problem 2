from collections import deque
import sys
 
def main():
data = sys.stdin.read().split()
 
n = int(data[0])
arr = list(map(int, data[1:n + 1]))
k = int(data[n + 1])
 
max_dq = deque()
min_dq = deque()
 
left = 0
best_len = 0
best_start = 0
 
for right in range(n):
while max_dq and arr[max_dq[-1]] < arrmax_dq.pop()
max_dq.append(right)
 
while min_dq and arr[min_dq[-1]] > arrmin_dq.pop()
min_dq.append(right)
 
while arr[max_dq[0]] - arr[min_dq[0]] > k:
if max_dq[0] == left:
max_dq.popleft()
if min_dq[0] == left:
min_dq.popleft()
left += 1
 
curr_len = right - left + 1
 
if curr_len > best_len:
best_len = curr_len
best_start = left
 
print(best_len, best_start + 1)
 
if __name__ == "__main__":
main()
Show more lines
